#include <stdio.h>
#include <stdlib.h>
/*getch peut être utilisée dans des jeux vidéos pour maintenir la dernière image en attendant que l'utilisateur 
presse une touche pour activer getch() qui met fin au programme.*/
int main(){
    printf("Enter a value: ");
    getch();
    return 0;
}

int main() {
    int value=0;
    printf("Enter a value: ");
    value=getch();
    for (i=0; i<V; i++) {
        printf("%d")
    }
}
int main() {
    Int int;
    printf("Enter a value V: ");
    scanf("%d", &Int);
    printf("Enter a value to display: ");
    INT int;
    scanf("%d", &INT);
    for (i=0; i<V; i++) {
        printf("%d" &INT);
    }
    return 0;
}